apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My workflow
on:
  push:
    branches:
      - "**"
  workflow_dispatch:
jobs:
  build:
    runs-on:
      - rama-label-1
      - rama-label-2
    steps:
      - name: Say hello
        shell: sh
        run: |
          echo "hello world"